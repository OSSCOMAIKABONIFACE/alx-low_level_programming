#include "main.h"
/**
*_islower - check that a char is a lower case
*@c: is the char to be checked
*Return: 1 if char us lower case,
*otherwuse 0.
*int_islower (int c)
*{
*if (c >='a' && c <=('2')
*else
*return (0);
